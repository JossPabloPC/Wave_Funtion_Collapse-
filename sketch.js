/// <reference path="TSDef/p5.global-mode.d.ts" />

var y = 0;

function setup() {
  noStroke();
  createCanvas(500, 450);
}


function draw() {
  background('darkblue');
  ellipse(width/2, Height ,50, 50);
}